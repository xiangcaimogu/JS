数组：存储：连续存储多个数据的存储空间
      使用：相当于多个变量的集合
why？数据的批量管理


null：一个变量没有指向任何对象。
何时使用null：主动释放对象！(这个对象不用了)主动释放的对象不能再被找回。
null vs undefined
null：专用于释放对象（主动）
undefined：专用于自动初始化--未赋值的变量


垃圾回收：js引擎自动销毁不再被引用的对象
垃圾回收程序

访问数组：数组：一组变量的集合
如何获得其中一个变量/数据：下标，数组中唯一标识一个元素的序号。从0开始。


js内置对象不但封装存储而且封装常用API
API：已经实现的方法！可以直接调用！
Array API：
1.arr.length属性：数组中的元素个数（length是一个属性）
    的
访问任意数组的最后一个元素：arr[arr.lenght-1]
length属性值，随数组长度变化而变化
固定用法：arr[arr.length]=新值